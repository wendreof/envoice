<%= form_with(model: envoice, local: true) do |form| %>
  <% if envoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(envoice.errors.count, "error") %> prohibited this envoice from being saved:</h2>

      <ul>
      <% envoice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id, id: :envoice_customer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
